[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "moj_projekt"
version = "0.1.0"
description = "Moja lokalna paczka"
dependencies = [] # Tutaj możesz wpisać biblioteki, np. "PyQt5", "numpy"